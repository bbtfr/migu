(function() {
  define(function(require) {
    "use strict";
    var Backbone, _;
    _ = require('underscore');
    Backbone = require('backbone');
    return Backbone.Model.extend({});
  });

}).call(this);
